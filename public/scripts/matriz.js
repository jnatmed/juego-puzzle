class Matriz {
    
    range(ini,fin){
        var a = new Array();
        for(var i = ini; ini <= fin; ini++){
            a.push(i);  
        }
        return a;       
    }
}

export default Matriz;